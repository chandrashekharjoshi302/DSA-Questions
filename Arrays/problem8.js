let veg=["Potato", "Tomato", "Egg Plant", "Cucumber",];


// we have a method called as "push".

// push will add the new element always at the end of the array.

veg.push("Cabbage");

console.log(veg);
console.log(veg.length);//5
console.log(veg[veg.length-1]);//Cabbage