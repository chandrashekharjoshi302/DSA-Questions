let arr=[
    [1,2,3],
    [4,5,6]
  ];
  
  console.log(arr.length); // 3 ==> no. of rows
  
  console.log(arr[0].length); //3 ==> no. of cols
  
  
  
  //==> If i calculate the length of array, it will give me the number of rows (matrix)
  
  //==> if i calculate the length of any elemnet if that array if will give me the no. of cols (matrix)